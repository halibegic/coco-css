#containers
    h2.docs-title.my-4.font-light
        | Containers
        a.anchor.ml-1.text-decoration-none(href="#containers") #
    p
        | Use #[code container-sm] (540px), #[code container-md] (720px), #[code container-lg] (960px), #[code container-xl] (1140px) or #[code container-xxl] (1320px) to the container for a fixed-width container with the specific max-width.
    pre.code(data-lang='HTML')
        code.docs-code
            :highlight(lang="html")
                <!-- 100% width container with max-width set to container-lg (960px) -->
                <div class="container container-lg">
                    <div class="row">
                    ...
                    </div>
                </div>
